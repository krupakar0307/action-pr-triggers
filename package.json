{
  "name": "notify-prs",
  "version": "1.0.0",
  "description": "GitHub Action to trigger workflows for all open pull requests",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "actions",
    "status",
    "check"
  ],
  "author": "Debuide",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "axios": "^1.8.0",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
