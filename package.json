{
  "name": "main-branch-status-check",
  "version": "1.0.0",
  "description": "GitHub Action to check if main branch is green before allowing PR to proceed",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "local-test": "node -e \"process.env.INPUT_GITHUB_TOKEN='your-token'; process.env.INPUT_MAIN_BRANCH='main'; process.env.GITHUB_REPOSITORY='your-username/your-repo'; require('./index.js')\""
  },
  "keywords": [
    "github",
    "actions",
    "status",
    "check"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  },
  "engines": {
    "node": ">=20"
  }
} 